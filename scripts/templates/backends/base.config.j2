bucket         = "terraform-state-{{ aws_account_id }}"
key            = "{{ project }}/{{ environment }}/{{ stack_name }}/terraform.tfstate"
region         = "ap-southeast-2"
dynamodb_table = "terraform-state-{{ aws_account_id }}"
encrypt        = true
